projection implementation in class YRAP_C_BP_MP_100_TRAV unique;
strict ( 2 );
use draft;
define behavior for YRAP_C_MP_100_TRAV alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action deductDiscount;
  use action copyTravel;
  use action acceptTravel;
  use action rejectTravel;
}